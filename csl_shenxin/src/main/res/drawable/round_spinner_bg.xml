<?xml version="1.0" encoding="utf-8"?>

<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <!-- 填充的颜色 -->
    <solid android:color="@color/spinner_title"/>
    <!-- android:radius 弧形的半径 -->
    <stroke android:color="@color/spinner_title_tab"  android:width="2dp"/>
    <corners
            android:bottomLeftRadius="14px"
            android:bottomRightRadius="14px"
            android:topRightRadius="14px"
            android:topLeftRadius="14px"/>
</shape>